import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.u2.cristhianbravo',
  appName: 'ph-u2-cristhianbravo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
